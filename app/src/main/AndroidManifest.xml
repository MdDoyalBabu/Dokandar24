<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dokandar24">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dokandar24">
        <activity android:name=".Seller.SellerRegistrationActivity"></activity>
        <activity android:name=".Seller.SellerLoginActivity" />
        <activity android:name=".Customer.CustomerRegistrationActivity" />
        <activity android:name=".Customer.CustomerLoginActivity" />
        <activity android:name=".Seller.SellerMainActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Customer.CustomerMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>